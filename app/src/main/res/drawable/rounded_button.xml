<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="12dp" />
    <solid android:color="@color/colorPurpleDark" />
    <elevation android:shadowColor="@color/colorPrimaryDark" android:radius="4dp" />

<corners android:radius="16dp" /> <!-- Adjust the radius for roundness -->
<solid android:color="@color/colorRedDark" /> <!-- Button color -->
</shape>

